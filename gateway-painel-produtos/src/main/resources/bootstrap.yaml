server:
  port: ${PORT:8081}

spring:
  application:
    name: gateway-painel-produtos
  cloud:
    config:
      uri: http://localhost:8888
      discovery:
        enabled: false
message:
  fallback: Teste